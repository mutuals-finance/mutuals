version: 2.1

parameters:
  run-shared:
    type: boolean
    default: false
  run-cms:
    type: boolean
    default: false
  run-landing:
    type: boolean
    default: false
  run-app:
    type: boolean
    default: false
  run-docs:
    type: boolean
    default: false
  run-contracts:
    type: boolean
    default: false

workflows:
  run-docs:
    jobs:
      - lint:
          name:  "[docs] lint"
      - test:
          name:  "[docs] test"
      - deploy: &base
          name: "[docs] deploy staging"
          vercel-project-id: $VERCEL_PROJECT_ID_DOCS
          vercel-org-id: $VERCEL_ORG_ID
          vercel-token: $VERCEL_TOKEN
          requires:
            - "[docs] lint"
            - "[docs] test"
          vercel-environment: preview
          filters:
            branches:
              ignore: /main/
      - deploy:
          <<: *base
          name: "[docs] deploy production"
          vercel-environment: production
          filters:
            branches:
              only: /main/
