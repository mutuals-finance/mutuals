version: 2.1

parameters:
  run-shared:
    type: boolean
    default: false
  run-landing:
    type: boolean
    default: false

workflows:
  docs-workflow:
    jobs:
      - lint
      - build:
          package: docs
