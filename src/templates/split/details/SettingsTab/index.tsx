import React from 'react';

import { SplitTemplateTabProps } from '#/split';

export function SettingsTab(props: SplitTemplateTabProps) {
  return (
    <section>
      <div className={'container'}>
        <h2 className={'title-3'}>Settings</h2>
      </div>
    </section>
  );
}
