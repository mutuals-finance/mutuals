/*
const baseStyle = definePartsStyle((props) => ({
  container: {
    group: {},
    label: {
      fontSize: "xs",
      color: props.colorMode === "dark" ? "gray.400" : "gray.600",
      fontWeight: "normal",
    },
  },
}));
*/
