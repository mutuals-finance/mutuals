import { PropsWithChildren } from "React"

export function {{ pascalCase name }}({ children }: PropsWithChildren) {
  return (
    <div>
      <h1>{{ pascalCase name }}</h1>
      {children}
    </div>
  );
}
