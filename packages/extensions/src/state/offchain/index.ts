import { Extension } from "../../types";

export type OffChainData = null;

export const defaultValue: OffChainData = null;

export const extension: Extension = {
  id: "offchain",
  name: "Off Chain",
  render: null,
};
