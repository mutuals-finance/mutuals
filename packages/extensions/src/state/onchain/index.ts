import { Extension } from "../../types";

export type OnChainData = null;

export const defaultValue: OnChainData = null;

export const extension: Extension = {
  id: "onchain",
  name: "On Chain",
  render: null,
};
