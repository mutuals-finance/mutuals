query PoolDetailsById($id: ID!) {
    split(id: $id) {
        id
        address
        blockNumber
        timestamp
        metaDataUri
        totalShares
        txCount
        metaData {
            name
            description
            image
        }
        shares {
            id
            payee
            timestamp
            value
        }
    }
}
